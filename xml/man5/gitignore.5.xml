<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE refentry PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd">
<?asciidoc-toc?>
<?asciidoc-numbered?>
<refentry>
<refmeta>
<refentrytitle>gitignore</refentrytitle>
<manvolnum>5</manvolnum>
<refmiscinfo class="source">Git</refmiscinfo>
<refmiscinfo class="manual">Git Manual</refmiscinfo>
</refmeta>
<refnamediv>
  <refname>gitignore</refname>
  <refpurpose>Specifies intentionally untracked files to ignore</refpurpose>
</refnamediv>
<refsynopsisdiv id="_synopsis">
<simpara>$HOME/.config/git/ignore, $GIT_DIR/info/exclude, .gitignore</simpara>
</refsynopsisdiv>
<refsect1 id="_description">
<title>DESCRIPTION</title>
<simpara>A <literal>gitignore</literal> file specifies intentionally untracked files that
Git should ignore.
Files already tracked by Git are not affected; see the NOTES
below for details.</simpara>
<simpara>Each line in a <literal>gitignore</literal> file specifies a pattern.
When deciding whether to ignore a path, Git normally checks
<literal>gitignore</literal> patterns from multiple sources, with the following
order of precedence, from highest to lowest (within one level of
precedence, the last matching pattern decides the outcome):</simpara>
<itemizedlist>
<listitem>
<simpara>
Patterns read from the command line for those commands that support
   them.
</simpara>
</listitem>
<listitem>
<simpara>
Patterns read from a <literal>.gitignore</literal> file in the same directory
   as the path, or in any parent directory, with patterns in the
   higher level files (up to the toplevel of the work tree) being overridden
   by those in lower level files down to the directory containing the file.
   These patterns match relative to the location of the
   <literal>.gitignore</literal> file.  A project normally includes such
   <literal>.gitignore</literal> files in its repository, containing patterns for
   files generated as part of the project build.
</simpara>
</listitem>
<listitem>
<simpara>
Patterns read from <literal>$GIT_DIR/info/exclude</literal>.
</simpara>
</listitem>
<listitem>
<simpara>
Patterns read from the file specified by the configuration
   variable <emphasis>core.excludesFile</emphasis>.
</simpara>
</listitem>
</itemizedlist>
<simpara>Which file to place a pattern in depends on how the pattern is meant to
be used.</simpara>
<itemizedlist>
<listitem>
<simpara>
Patterns which should be version-controlled and distributed to
   other repositories via clone (i.e., files that all developers will want
   to ignore) should go into a <literal>.gitignore</literal> file.
</simpara>
</listitem>
<listitem>
<simpara>
Patterns which are
   specific to a particular repository but which do not need to be shared
   with other related repositories (e.g., auxiliary files that live inside
   the repository but are specific to one user&#8217;s workflow) should go into
   the <literal>$GIT_DIR/info/exclude</literal> file.
</simpara>
</listitem>
<listitem>
<simpara>
Patterns which a user wants Git to
   ignore in all situations (e.g., backup or temporary files generated by
   the user&#8217;s editor of choice) generally go into a file specified by
   <literal>core.excludesFile</literal> in the user&#8217;s <literal>~/.gitconfig</literal>. Its default value is
   $XDG_CONFIG_HOME/git/ignore. If $XDG_CONFIG_HOME is either not set or
   empty, $HOME/.config/git/ignore is used instead.
</simpara>
</listitem>
</itemizedlist>
<simpara>The underlying Git plumbing tools, such as
<emphasis>git ls-files</emphasis> and <emphasis>git read-tree</emphasis>, read
<literal>gitignore</literal> patterns specified by command-line options, or from
files specified by command-line options.  Higher-level Git
tools, such as <emphasis>git status</emphasis> and <emphasis>git add</emphasis>,
use patterns from the sources specified above.</simpara>
</refsect1>
<refsect1 id="_pattern_format">
<title>PATTERN FORMAT</title>
<itemizedlist>
<listitem>
<simpara>
A blank line matches no files, so it can serve as a separator
   for readability.
</simpara>
</listitem>
<listitem>
<simpara>
A line starting with # serves as a comment.
   Put a backslash ("<literal>\</literal>") in front of the first hash for patterns
   that begin with a hash.
</simpara>
</listitem>
<listitem>
<simpara>
Trailing spaces are ignored unless they are quoted with backslash
   ("<literal>\</literal>").
</simpara>
</listitem>
<listitem>
<simpara>
An optional prefix "<literal>!</literal>" which negates the pattern; any
   matching file excluded by a previous pattern will become
   included again.
   Put a backslash ("<literal>\</literal>") in front of the first "<literal>!</literal>" for patterns
   that begin with a literal "<literal>!</literal>", for example, "<literal>\!important!.txt</literal>".
   It is possible to re-include a file if a parent directory of that
   file is excluded if certain conditions are met. See section NOTES
   for detail.
</simpara>
</listitem>
<listitem>
<simpara>
If the pattern ends with a slash, it is removed for the
   purpose of the following description, but it would only find
   a match with a directory.  In other words, <literal>foo/</literal> will match a
   directory <literal>foo</literal> and paths underneath it, but will not match a
   regular file or a symbolic link <literal>foo</literal> (this is consistent
   with the way how pathspec works in general in Git).
</simpara>
</listitem>
<listitem>
<simpara>
If the pattern does not contain a slash <emphasis>/</emphasis>, Git treats it as
   a shell glob pattern and checks for a match against the
   pathname relative to the location of the <literal>.gitignore</literal> file
   (relative to the toplevel of the work tree if not from a
   <literal>.gitignore</literal> file).
</simpara>
</listitem>
<listitem>
<simpara>
Otherwise, Git treats the pattern as a shell glob suitable
   for consumption by fnmatch(3) with the FNM_PATHNAME flag:
   wildcards in the pattern will not match a / in the pathname.
   For example, "Documentation/&#42;.html" matches
   "Documentation/git.html" but not "Documentation/ppc/ppc.html"
   or "tools/perf/Documentation/perf.html".
</simpara>
</listitem>
<listitem>
<simpara>
A leading slash matches the beginning of the pathname.
   For example, "/&#42;.c" matches "cat-file.c" but not
   "mozilla-sha1/sha1.c".
</simpara>
</listitem>
</itemizedlist>
<simpara>Two consecutive asterisks ("<literal>**</literal>") in patterns matched against
full pathname may have special meaning:</simpara>
<itemizedlist>
<listitem>
<simpara>
A leading "<literal>**</literal>" followed by a slash means match in all
   directories. For example, "<literal>**/foo</literal>" matches file or directory
   "<literal>foo</literal>" anywhere, the same as pattern "<literal>foo</literal>". "<literal>**/foo/bar</literal>"
   matches file or directory "<literal>bar</literal>" anywhere that is directly
   under directory "<literal>foo</literal>".
</simpara>
</listitem>
<listitem>
<simpara>
A trailing "<literal>/**</literal>" matches everything inside. For example,
   "<literal>abc/**</literal>" matches all files inside directory "<literal>abc</literal>", relative
   to the location of the <literal>.gitignore</literal> file, with infinite depth.
</simpara>
</listitem>
<listitem>
<simpara>
A slash followed by two consecutive asterisks then a slash
   matches zero or more directories. For example, "<literal>a/**/b</literal>"
   matches "<literal>a/b</literal>", "<literal>a/x/b</literal>", "<literal>a/x/y/b</literal>" and so on.
</simpara>
</listitem>
<listitem>
<simpara>
Other consecutive asterisks are considered invalid.
</simpara>
</listitem>
</itemizedlist>
</refsect1>
<refsect1 id="_notes">
<title>NOTES</title>
<simpara>The purpose of gitignore files is to ensure that certain files
not tracked by Git remain untracked.</simpara>
<simpara>To stop tracking a file that is currently tracked, use
<emphasis>git rm --cached</emphasis>.</simpara>
<simpara>To re-include files or directories when their parent directory is
excluded, the following conditions must be met:</simpara>
<itemizedlist>
<listitem>
<simpara>
The rules to exclude a directory and re-include a subset back must
   be in the same .gitignore file.
</simpara>
</listitem>
<listitem>
<simpara>
The directory part in the re-include rules must be literal (i.e. no
   wildcards)
</simpara>
</listitem>
<listitem>
<simpara>
The rules to exclude the parent directory must not end with a
   trailing slash.
</simpara>
</listitem>
<listitem>
<simpara>
The rules to exclude the parent directory must have at least one
   slash.
</simpara>
</listitem>
</itemizedlist>
</refsect1>
<refsect1 id="_examples">
<title>EXAMPLES</title>
<literallayout class="monospaced">    $ git status
    [...]
    # Untracked files:
    [...]
    #       Documentation/foo.html
    #       Documentation/gitignore.html
    #       file.o
    #       lib.a
    #       src/internal.o
    [...]
    $ cat .git/info/exclude
    # ignore objects and archives, anywhere in the tree.
    *.[oa]
    $ cat Documentation/.gitignore
    # ignore generated html files,
    *.html
    # except foo.html which is maintained by hand
    !foo.html
    $ git status
    [...]
    # Untracked files:
    [...]
    #       Documentation/foo.html
    [...]</literallayout><simpara></simpara>
<simpara>Another example:</simpara>
<literallayout class="monospaced">    $ cat .gitignore
    vmlinux*
    $ ls arch/foo/kernel/vm*
    arch/foo/kernel/vmlinux.lds.S
    $ echo '!/vmlinux*' &gt;arch/foo/kernel/.gitignore</literallayout><simpara></simpara>
<simpara>The second .gitignore prevents Git from ignoring
<literal>arch/foo/kernel/vmlinux.lds.S</literal>.</simpara>
<simpara>Example to exclude everything except a specific directory <literal>foo/bar</literal>
(note the <literal>/*</literal> - without the slash, the wildcard would also exclude
everything within <literal>foo/bar</literal>):</simpara>
<literallayout class="monospaced">    $ cat .gitignore
    # exclude everything except directory foo/bar
    /*
    !/foo
    /foo/*
    !/foo/bar</literallayout><simpara></simpara>
</refsect1>
<refsect1 id="_see_also">
<title>SEE ALSO</title>
<simpara><citerefentry>
<refentrytitle>git-rm</refentrytitle><manvolnum>1</manvolnum>
</citerefentry>,
<citerefentry>
<refentrytitle>gitrepository-layout</refentrytitle><manvolnum>5</manvolnum>
</citerefentry>,
<citerefentry>
<refentrytitle>git-check-ignore</refentrytitle><manvolnum>1</manvolnum>
</citerefentry></simpara>
</refsect1>
<refsect1 id="_git">
<title>GIT</title>
<simpara>Part of the <citerefentry>
<refentrytitle>git</refentrytitle><manvolnum>1</manvolnum>
</citerefentry> suite</simpara>
</refsect1>
</refentry>
